import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    height: 56,
    width: '100%',
    backgroundColor: '#6016FC',
    justifyContent: 'center',
    alignItems: 'center',
    borderRadius: 5, 
    marginBottom: 70,
    
  },
  title: {
    fontWeight: 'bold',
    color: '#FFFF',
    fontSize: 15,
  }
});
